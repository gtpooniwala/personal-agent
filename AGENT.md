# AGENT.md

Compatibility shim for tooling or docs that still reference `AGENT.md`.

Source of truth:
- Follow [`AGENTS.md`](AGENTS.md) for the current root-checkout, slot, branch, and PR workflow.

Legacy compatibility:
- Keep this file in place so older scripts/docs do not break while the repo migrates to `AGENTS.md`.
